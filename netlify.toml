[build]
  command = "python -m pip install -r requirements.txt"
  publish = "."
  functions = "."

[functions]
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
